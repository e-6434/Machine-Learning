##import tkinter as tk

```
import tkinter as tk


---

# نکات نگارشی برای توضیحات
- از فعل‌های معلوم و زمان حال استفاده کن.
- کوتاه و مشخص باش (هر توضیح 1–2 جمله).
- اگر خطی وابسته به محیط/سِت‌آپ باشه (مثلاً `process.env` یا API key)، حتماً تذکر بده.
- برای توابع مهم، اضافه کن: «پارامترها»، «مقدار بازگشتی»، «موارد خطا».

---

اگر می‌خوای فوراً شروع کنم:
- کدت رو اینجا پیست کن (تا من خط‌به‌خط توضیح بدم)، یا
- بگو چه زبان/فریم‌ورکی هست تا من قالب نهایی README با مثال‌های کامل‌تر (و استایل و ایموجی و بخش‌های مربوطه) تولید کنم.

دوست داری کد رو بفرستی یا اول یه README نمونه کامل بخوام برات بسازم؟
